[pytest]
DJANGO_SETTINGS_MODULE=config.settings.test
norecursedirs = node_modules
addopts = --reuse-db -m "not stripe"
markers =
    stripe: hits a real Stripe sandbox; skipped unless STRIPE_SANDBOX_SECRET_KEY is set
