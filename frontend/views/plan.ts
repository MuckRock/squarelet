/**
 * Plan page view
 *
 * This file is kept minimal as the form-specific logic has been moved to
 * plan_purchase_form.ts which is loaded by the PlanPurchaseForm template.
 *
 * Page-level CSS is imported here for non-form elements (header, benefits, etc.)
 */

import "@/css/plan.css"

// Page-level initialization (if needed in the future)
document.addEventListener("DOMContentLoaded", function () {
  // Any page-level initialization that's not form-specific goes here
});
